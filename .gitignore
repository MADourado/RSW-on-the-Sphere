# Python
**/__pycache__/
*.pyc
*.egg-info/
build/

# Virtual environment
.venv/

# Generated figures / outputs (regenerate via main.py and the plotting scripts)
outputs/
# Pre-driver-refactor backup of outputs/ (see .claude/PLAN-driver-refactor-2026-08-25.md
# Phase 0) -- temporary, removable once Phase B+C's regression diffs pass.
outputs_legacy/
*.png
*.gif
*.npy
examples/*.tex

# Superseded/archived scripts and their outputs -- a holding area, not meant
# to gain new git history. Files already tracked here from before this rule
# stay tracked; this only blocks NEW additions (git add refuses an ignored
# path without -f).
examples/legacy/

# Claude Code project-local context (not shared repo documentation)
.claude/

#paper (separate repository for paper)
paper-nonlinear-interactions-SWE-sphere/