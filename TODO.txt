TODO

Efficiency-variation rework (replaces P-measure; plan agreed 2026-09-03):
- eq. 3.26: tighten the definition and notation so it matches section 4.1.1 later. Don't call |A_a|^2 kinetic energy -- it's the full quadratic energy (potential+kinetic); just note it resembles the 2022 paper's own kinetic energy. Settle notation for "a" here to match whatever section 4.1.1 ends up using.
- section 4.1.1: full rewrite.
-- condense the total-energy/dimensional-conversion part (currently way too long) into a single paragraph + 1-2 equations, leading into the energy-variation Delta E definition.
-- define the new efficiency variation (single diagnostic, replacing today's separate P-measure -- P-measure IS efficiency variation once both sides share the same reference energy budget, so keep only one symbol/name; no separate D term needed in the final formula, it cancels).
-- reduce the dominant-period description: just state that a novel period is a peak present in the quartet's own spectrum but absent from the constituent triads' spectra -- always checked by eye, no need to spell out an automated detection algorithm in the text.
-- comment out (don't delete) the Energy variation (P_a) part and the spectral deviation (D_2^a) part.
- add a short paragraph near the diagnostics section (where the energy/novelty spectra are discussed, e.g. §sec: diagnostics / around fig: cap4ex1) explaining why a mode's own linear frequency omega_a never appears as a peak in these spectra: E_a=|A_a|^2's time derivative has zero contribution from the -i*omega_a*A_a term (pure phase rotation, eq: da2/3.23), for any coupling strength -- only the nonlinear coupling terms (and the mismatch delta, via the dynamical phase Phi) set the energy-exchange periods actually seen in the spectrum. Point out this holds identically for the quartet system (eq 4.1-4.4), not just the triad case.
- recalculate the numbers in Table 3.
- be very clear, everywhere efficiency/efficiency variation is reported in the paper, with respect to which triad/quartet (and whether "alone" or embedded) each number is measured against.
- Figure 7: re-derive the efficiency panel to see how it actually looks. E_avg was never properly defined -- check whether it should be the (new) efficiency variation instead, or whether it's meant to match Raphaldini 2022's own calculation.
- re-calculate everything in section 4.3.
- Figure 8: font too small to read.
- Figure 9: reduce to a single plot with 2 y-axes -- the coupling coefficient values and the (new) efficiency variation for RH(1,2), nothing else.
- Figures 10, 11, 12: same font fix as Figure 8, plus switch to the new efficiency variation.
- general: figure fonts too small, especially multi-panel figures.

- registry cleanup: `quartet_gravity_79` (wave_sets_default.yaml) uses EG(7,9), display_label "Quartet D" -- collides with the real Quartet D (`quartet_rossby_gravity_influence`, WG(7,9)/WG(3,9), the one in Figure 5 panel d / Table quartet_master / Figures 010-011, verified matching). `quartet_gravity_79` is unused by any current figure/table; only consumer is `examples/tables/paper_headlines_sec3.3.py`, whose docstring claims its "Quartet D (EG(7,9))" headline numbers are cited in sec: quartet_rossby_gravity_fast -- they aren't anymore (superseded by Quartet E, added later). Rename/remove `quartet_gravity_79`'s display_label and drop the stale headline Quartet D call+docstring note.

- change author order: peixoto, dourado, teruya...raphaldini last as corresponding.

quintets:
- re-test current quintet with Kelvin mode quintet_gravity_star. quintet A should have quartet C + EG(7,9)
- build a new quintet (B) based on quartet D, adding an EG mode 3,9.
- add in paper the diagram with these quintet
- add a table similar to table 2 for the quintet.
- add analysis in paper for these 2 quintets:
-- panels containing for each quintet:
--- evolution of each individual triad (3-top row)
--- evolution of quintet and spectra of 2 rossby modes, RH(1,2) and RH(4,5) for quintet A and for quintet B RH(3,4) RH(4,5)
- later we will include other analysis for these quintets.
